import { ShringAmountPipe } from './shring-amount.pipe';

describe('ShringAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new ShringAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
