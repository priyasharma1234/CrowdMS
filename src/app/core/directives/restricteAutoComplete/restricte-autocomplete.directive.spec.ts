import { RestricteAutocompleteDirective } from './restricte-autocomplete.directive';

describe('RestricteAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new RestricteAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
